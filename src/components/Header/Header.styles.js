import styled from "styled-components";

export const HeaderContainer = styled.header`
  padding: 1rem 0;
  .header-title {
    font-weight: 400;
    color: #fea141;
    margin: 1rem 0;
    font-size: 1.2rem;
  }
  .header-subtitle {
    font-weight: 400;
    color: #ffff;
    margin: 2rem 0;
    font-size: 1.4rem;
    .subtitle-span {
      font-weight: 500;
    }
  }
`;
